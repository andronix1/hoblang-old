#`
  this is multiline comments lol!!!
  you can write anything here except # after `, but you can just write `
`#

fun test() -> void {
    var NULL = null;
    if 0.0 {
        (0) = 10;
    }
    return 0.0;
}

fun main() -> i32 {
    i32.super();
        ("OK").duper();
            (0.1 + 1);
    ("super")();
    return;
    return 0.0;
}
