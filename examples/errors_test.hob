#`
  this is multiline comments lol!!!
  you can write anything here except # after `, but you can just write `
`#

struct Test {
    a: i32,
    b: i64
}

fun test() -> void {
    var t = struct Test {
        a: 12
    };
    var t = struct Test {
        a: 12,
        b: 123,
        c: 90
    };
    var NULL = null;
    if 0.0 {
        (0) = 10;
    }
    return 0.0;
}

fun main() -> i32 {
    i32.super();
        ("OK").duper();
            (0.1 + 1);
    ("super")();
        (1 as []u8);
    return;
    return 0.0;
}
