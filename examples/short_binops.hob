import "std/io.hob" as io;
import "std/process.hob" as process;

fun main() -> i32 {
    var i: i32 = 0;
    while i < 10 {
        io.print_i32(i);
        io.println("");
        i += 1;
    }
    return 0;
}

fun _start() -> void {
    process.exit(main());
}
